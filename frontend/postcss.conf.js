module.exports = {
    plugins: {
      // autoprefixer: {}, // Angular CLI handles autoprefixer by default based on browserslist
      // You can add other PostCSS plugins here if needed, for example:
      // 'postcss-import': {},
      // 'tailwindcss': {},
    }
  };