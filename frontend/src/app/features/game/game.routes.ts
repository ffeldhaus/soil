import { Routes } from '@angular/router';

export const GAME_ROUTES: Routes = [
  // Add game-related routes here, for example:
  // { path: '', component: GameLobbyComponent },
  // { path: 'play', component: GamePlayComponent },
];
